以下は{date}の天気ダイジェスト記事の下書きです。「気象庁データ」と突き合わせてファクトチェックし、修正済みの最終版だけを出力してください。

チェック項目:
- 「各地の天気」「予想気温」内のすべての数値が気象庁データと一致するか。一致しない数値は修正するか、確認できなければ削除する。
- 記事全体を通して、気象庁データにない観測値・予想値・警報や注意報・アラートの発表状況・対象地域数が書かれていないか。あれば削除する。
- 「今日の天気解説」セクション（今日の主役・なぜそうなるのか・今日どう動くか）は、記事の核なので削除・要約・短縮をしない。仕組みの説明・たとえ・定性的な見通しはそのまま残す。ただしセクション内に気象庁データにない具体的な数値があれば、その数値だけを定性的な表現（「厳しい暑さ」「警報級のおそれ」等）に置き換える。
- 「記録的」「観測史上」などデータに根拠のない誇張表現を削除する。
- 記事中のすべてのURL（今日のポイント・今日の天気解説・関連ニュースを含む）が、入力のニュースに一字一句同じ形で実在するか。入力にないURL、改変されたURLは、リンクごと削除する。

修正点の説明や差分は書かず、最終版の記事（Markdown）のみを出力すること。

# 気象庁データ（事実の唯一の根拠）
{jma_block}

# 下書き
{draft}
