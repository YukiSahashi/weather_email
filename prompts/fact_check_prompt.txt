以下は{date}の天気ダイジェスト記事の下書きです。「気象庁データ」と突き合わせてファクトチェックし、修正済みの最終版だけを出力してください。

チェック項目:
- 下書き中のすべての数値（気温・降水確率など）が気象庁データと一致するか。一致しない数値は気象庁データの値に修正するか、確認できなければ削除する。
- 気象庁データに存在しない警報・注意報・地名・数値が書かれていないか。あれば削除する。
- 「記録的」「観測史上」などデータに根拠のない誇張表現がないか。あれば削除する。
- 関連ニュースのURLが入力のニュースに実在するものか。入力にないURLは削除する。

修正点の説明や差分は書かず、最終版の記事（Markdown）のみを出力すること。

# 気象庁データ（事実の唯一の根拠）
{jma_block}

# 下書き
{draft}
