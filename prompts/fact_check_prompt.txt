以下は{date}の天気ダイジェスト記事の下書きです。「気象庁データ」と突き合わせてファクトチェックし、修正済みの最終版だけを出力してください。

チェック項目:
- 「全国の概況」「東日本」「西日本」「予想気温」内のすべての数値が気象庁データと一致するか。一致しない数値は修正するか、確認できなければ削除する。
- 記事全体を通して、気象庁データにない観測値・予想値・警報や注意報・アラートの発表状況・対象地域数が書かれていないか。あれば削除する。
- 「解説と見解」セクションは削除しない。ただしセクション内の具体的な数値（気温・雨量・確率など）が気象庁データにないものであれば、定性的な表現（「厳しい暑さ」「警報級のおそれ」等）に置き換える。
- 「記録的」「観測史上」などデータに根拠のない誇張表現を削除する。
- 関連ニュースのURLが入力のニュースに実在するか。入力にないURLは削除する。

修正点の説明や差分は書かず、最終版の記事（Markdown）のみを出力すること。

# 気象庁データ（事実の唯一の根拠）
{jma_block}

# 下書き
{draft}
